[project]
name = "equalizer-demo"
version = "0.1.0"
description = "Tk demo driving the graphic equalizer in real time"
readme = "README.md"
requires-python = ">=3.14"
license = "MIT"
dependencies = [
    "activator",
    "equalizer",
    "pyyaml>=6.0",
    "scipy>=1.18.1",
]

[tool.uv.sources]
activator = { path = "../activator", editable = true }
equalizer = { path = "../equalizer", editable = true }
